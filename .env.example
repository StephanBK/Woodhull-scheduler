# For local dev (SQLite) — comment out and Railway sets DATABASE_URL automatically
# DATABASE_URL=sqlite:///./woodhull.db

# For local dev against a Postgres docker container, e.g.:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/woodhull
